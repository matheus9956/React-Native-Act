import React from "react";
import { View, StyleSheet, Text } from "react-native";

const RegisterPage = () => {
  return (
    <View>
      <Text>register page screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default RegisterPage;
